# unwritten-messages-entropy-detection
This example demonstrates how information entropy can be used to detect 'unwritten messages' or hidden patterns within seemingly random data. It generates truly random data, data with a subtle character bias, and data with an embedded repeating pattern, then calculates and compares their Shannon entropy values. Lower entropy indicates more predicta
